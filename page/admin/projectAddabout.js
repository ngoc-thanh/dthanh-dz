import axios from "axios";
import { router, useEffect, useState } from "../../lib";
import { addAbouts } from "./config/abouts";

const projectAddabout = () => {
    const [About, setAbout] = useState([])
    useEffect(()=>{

    },[])

    return `
    
  `
}

export default projectAddabout;